// ApiError is a custom error class that extends the native Error
// Import it from: import { ApiError } from '../../../src/utils/ApiError.js'
// Usage: const err = new ApiError(404, 'Not found');

describe('ApiError — custom error class', () => {
    test.todo('should be an instance of Error');

    test.todo('should set statusCode correctly on the instance');

    test.todo('should set message correctly on the instance');

    test.todo('should set success to false by default');

    test.todo('should accept an optional errors array as the third argument');

    test.todo('should default errors to an empty array when not provided');
});
